---
layout: layouts/base.njk
section: home
---
<div class="home-page">
	<div class="home-page__bg-square"></div>

  <div class="hero">
    <div class="hero__col hero_container">
      <h1 class="hero__title gradient-text">Choose </h1>
      <span class="content__container">
        <ul class="hero__title content__container__list">
          <h1 class="hero__title gradient-text content__container__list__item xl:pl-3">Nourish</h1>
          <h1 class="hero__title gradient-text content__container__list__item xl:pl-3">Gluten-Free<h1>
          <h1 class="hero__title gradient-text content__container__list__item xl:pl-3">Vegetarian<h1>
          <h1 class="hero__title gradient-text content__container__list__item xl:pl-3">Low Iodine<h1>
          <h1 class="hero__title gradient-text content__container__list__item xl:pl-3">IBS Friendly<h1>            
          <h1 class="hero__title gradient-text content__container__list__item xl:pl-3">Paelo<h1>  
          <h1 class="hero__title gradient-text content__container__list__item xl:pl-3">Nourish<h1>
        </ul>
      </span>

      {# <h1 class="hero__title gradient-text">{{ home.title }}</h1>
      {% if home.subtitle %}
        <h2 class="hero__subtitle">{{ home.subtitle }}</h2>
      {% endif %} #}
    </div>
    <div class="hero__col">
      <img
        class="hero__image shadow"
        src="{% if home.image %}{{ home.image }}{% else %}https://source.unsplash.com/random/600x600{% endif %}"
        alt="{{ home.image_alt }}"
        width="1000px"
        height="1000px"
        loading="lazy">
    </div>
  </div>

<h2>Testing for Recipe Search</h2>
<ul>
{% for recipe in collections.recipes %} 
  <li><a href="{{ recipe.url }}">{{ recipe.data.title }}</a></li>
{% endfor %}
</ul>

<div class="search-container">
    <form action="work">
      <input type="search" placeholder="Search.." name="work">
      <button type="submit"><i class="btnSearch">Search</i></button>
    </form>
</div>

{# <div>
{% if results %}
<h3>Search Results</h3>
<ul>
  <li class="result-list">
    {%- if search != "recipes" -%}
    {% for result in results %}
    {% set resultUrl %}/recipes/{{ tag }}/{% endset %}
      <a class="tag" href="{{ resultUrl | url }}" rel="recipe">{{ recipe }}</a>
      {%- endif -%}
    {% endfor %}
  </li>
{% endif %}
</div> #}
  <hr>

  <h2>Recipes</h2>
  {% set projectlist = collections.project %}
  {% include "components/projectlist.njk" %}


  <hr>
  <h2>Recipes</h2>
  {% set postslist = collections.post %}
  {% set show_postslist_tags = false %}
  {% set limit = 3 %}
  {% include "components/postslist.njk" %}
</div>
